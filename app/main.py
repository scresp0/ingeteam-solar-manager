"""
main.py — punto de entrada del contenedor.
En Fase 2 solo arranca y confirma que el entorno está OK.
"""
import sys

def main():
    print("solar-manager arrancando...", flush=True)
    print(f"Python {sys.version}", flush=True)
    print("Entorno OK — pendiente de implementación (Fase 3)", flush=True)

if __name__ == "__main__":
    main()
